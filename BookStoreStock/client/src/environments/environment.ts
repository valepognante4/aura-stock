// environment.ts — Configuración para desarrollo (ng serve)
export const environment = {
  production: false,
  // En desarrollo, el backend corre manualmente o como sidecar en el mismo puerto
  apiUrl: 'http://127.0.0.1:8000',
};
